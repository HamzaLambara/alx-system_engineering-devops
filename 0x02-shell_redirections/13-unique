#!/bin/bash

# Read input from stdin and count occurrences of each word
readarray -t words
declare -A counts
for word in "${words[@]}"; do
    counts["$word"]=$((counts["$word"]+1))
done

# Print words that appear exactly once
for word in "${!counts[@]}"; do
    if (( counts["$word"] == 1 )); then
        echo "$word"
    fi
done | sort
